import { loaderOptions } from '../core/loader';

/**
 * @default
 * loaderOptions: {
 *   includeRegExp: /\.md$/i,
 * }
 */
export default function mdLoader(
  requireContext: any,
  loaderOptions?: loaderOptions,
): void;
